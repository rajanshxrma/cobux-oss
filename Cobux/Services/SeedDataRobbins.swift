import SwiftUI
import SwiftData

/// Stubbed for the same reason as `SeedDataMicrobiology.swift` -- see its doc comment.
extension SeedData {
    static func seedRobbins(modelContext: ModelContext) {
        // No-op in the public repo -- see SeedDataMicrobiology.swift's doc comment.
    }
}
