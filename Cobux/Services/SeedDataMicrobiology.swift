import SwiftData

// Stubbed in the public repo: the seed content is copyrighted book material and
// does not ship publicly. The loader and schema stay so the app builds.
extension SeedData {
    static func seedMicrobiology(modelContext: ModelContext) {}
}
